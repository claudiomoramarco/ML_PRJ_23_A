"""
__init__.py per il pacchetto sourcecode

Questo file rende Python in grado di riconoscere la directory 'sourcecode' come un pacchetto Python. 
È intenzionalmente lasciato vuoto per permettere l'importazione di tutti i moduli all'interno 
del pacchetto senza restrizioni specifiche.

Puoi importare qualsiasi modulo direttamente da 'sourcecode', ad esempio:
from sourcecode.modulo import ClasseOggetto

Se necessario, questo file può essere utilizzato per controllare quali moduli vengono esportati 
quando si importa il pacchetto sourcecode, o per eseguire codice di inizializzazione specifico per il pacchetto.
"""